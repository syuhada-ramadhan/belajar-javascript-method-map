// array nomor
const nomor = [1, 4, 5, 8, 10];

// cetak array nomor
console.log(nomor);

// kali dua setiap elemen array nomor
const kaliDua = nomor.map((elemen) => elemen * 2);

// cetak hasil variabel kaliDua
console.log(kaliDua);
